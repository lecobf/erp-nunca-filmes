# ============================================================
# ⚙️ CONFIGURAÇÃO DO SISTEMA ERP BACKEND
# ============================================================

# 🔹 Porta padrão do servidor FastAPI / Uvicorn
PORT=8000

# ============================================================
# 💾 BANCO DE DADOS
# ============================================================

# 🔸 Caminho absoluto do banco de dados SQLite
#   Linux/macOS: use algo como /srv/dropbox/nunca_filmes/pj/erp/dados/nunca.db
#   Windows:     use algo como F:\Dropbox\NUNCA_FILMES\PJ\erp\dados\nunca.db
ERP_DB_PATH=F:/Dropbox/NUNCA_FILMES/PJ/erp/app/dados/nunca.db



# ============================================================
# 🌐 CORS (Cross-Origin Resource Sharing)
# ============================================================

# 🔸 Origem(s) permitida(s) a acessar a API.
#   Separe múltiplas origens por vírgula.
#   Exemplo: http://localhost:5173,http://192.168.0.110:5173
CORS_ORIGINS=http://localhost:5173,http://192.168.0.110:5173

# ============================================================
# 🧱 OUTRAS OPÇÕES (opcional)
# ============================================================

# 🔸 Ambiente de execução (ex: development / production)
ENVIRONMENT=development

# 🔸 Nome de exibição da aplicação (opcional)
APP_NAME=ERP Backend

# ============================================================
# 🧩 OBSERVAÇÕES
# ============================================================
# 1. Este arquivo deve ficar na raiz do projeto (ex: ./app/.env)
# 2. Nunca envie o .env para repositórios públicos.
# 3. Para carregar automaticamente, instale o python-dotenv:
#      pip install python-dotenv
#    e adicione no main.py (logo no início):
#      from dotenv import load_dotenv; load_dotenv()
# 4. Uvicorn já lê variáveis automaticamente do .env se estiver na raiz.
