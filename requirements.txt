# ============================================================
# 🎬 ERP NUNCA FILMES - Backend FastAPI
# Python 3.12 / Render.com
# ============================================================

# --- Framework principal ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Banco de dados / ORM ---
SQLAlchemy==2.0.36
alembic==1.13.3

# 🔹 Drivers PostgreSQL (com fallback automático)
psycopg[binary]

sqlite-utils==3.37          # Utilitário útil em dev/testes

# --- Validação e configuração ---
pydantic>=2.9
pydantic-settings
python-dotenv==1.0.1

# --- Segurança e CORS ---
starlette==0.37.2
cryptography==43.0.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Utilitários gerais ---
requests==2.32.3
python-multipart==0.0.9
typing-extensions==4.12.2

# --- Logs e monitoramento ---
loguru==0.7.3

# --- Migração e ETL ---
pandas==2.2.3
openpyxl==3.1.5

# --- Testes ---
pytest==8.3.3
httpx==0.27.2

# --- Compatibilidade Render / Deploy ---
gunicorn==23.0.0

# ============================================================
# Observações:
# - Agora inclui fallback: psycopg → psycopg2
# - psycopg[binary] é usado no Render (não requer compilação)
# - pydantic-settings e dotenv para ambientes dinâmicos
# - loguru para logs avançados em produçãoo
# ============================================================
